int var = 4;
int[64] var2;
for (int i = 0; i < 64; i++)
{
    var2[i] = var;
}

string string_fun(int a, bool b, string c)
{
    if b
        return string_of_int(a) + c;
    else
        return c;
}

string str = "here's a string";
string other_str = string_fun(9, true, str);

while (var < 9 && true or false)
    var ++;

little_fun = lambda () return little_fun();


